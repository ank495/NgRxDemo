import { loginState } from "../login/state/login.reducer";

// Only the non lazy loaded component.
export interface State {
  //login: loginState As i is lazy loaded we need to write in the model of component.
}
